/**
 * Kept in one place because the client has not signed off on the name. When it
 * changes, this is the only edit — header, page titles and metadata all read
 * from here.
 */
export const SHOP_NAME = 'Phone Se Mangao';

/** The line under the wordmark in the design's opening screen. */
export const SHOP_STRAPLINE = 'Fresh · Daily · At your door';

export const SHOP_TAGLINE = 'Fresh fruits, vegetables and grocery, delivered on your schedule.';

/** localStorage keys. Namespaced so they cannot collide with anything else. */
export const CART_STORAGE_KEY = 'pm.cart.v1';
export const PINCODE_STORAGE_KEY = 'pm.pincode.v1';
